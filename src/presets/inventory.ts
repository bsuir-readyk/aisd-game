const SIZE = 32;

export type TInventoryItem = {
    name: string;
    img: string;
}

// const SimpleKeyImage = new Image()
// SimpleKeyImage.src = ;
export const SimpleKey: TInventoryItem = {
    name: "SimpleKey",
    img: "./simpleKey.jpg",
}